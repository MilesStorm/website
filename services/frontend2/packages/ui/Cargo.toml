[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true }
api = { workspace = true }
dioxus-sdk = { version = "^0.7.0", features = ["storage"] }

[features]
server = ["api/server"]
